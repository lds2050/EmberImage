node_modules/
dist/
release/
out/
coverage/
.DS_Store
npm-debug.log*
*.log

# Local credentials and environment files must never enter Git.
.env
.env.*
!.env.example
.npmrc
*.pem
*.p12
*.pfx
*.key
credentials.json
secrets.json

# Locally generated installers and update metadata.
*.dmg
*.exe
*.blockmap

# Internal docs stay local; never publish to GitHub (release notes and README excepted).
docs/PRD-*.md
docs/HANDOFF-*.md

# Local AI assistant workspace (memory, session state) — never enter Git.
.workbuddy/
overview.md
